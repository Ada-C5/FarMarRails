<h1> Create a new product: </h1>

<%= form_for @product do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.hidden_field :vendor_id, :value => @vendor.id %> <br>
  <%= f.submit %>
<% end %>
