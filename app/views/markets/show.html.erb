<h1><%= @market.name %></h1>
<ul class="list-inline">

  <li>
    <%= @market.address %>,<br>
    <%= @market.city %>, <%= @market.county %>, <%= @market.state %>, <%= @market.zip %>
  </li>

  <li>
    <%= link_to "", edit_market_path, class:"glyphicon glyphicon-pencil", alt: "Edit Market"  %>
  </li>

</ul>

<h2>All Vendors at this market!</h2>
<ul class="dl-horizontal">

<% @market.vendors.each do |vendor| %>

  <li>
    <%= link_to vendor.name, vendor_path(vendor.id) %>
  </li>

  <ul class="list-inline">

    <li>
      <%= link_to "",  vendor_path(vendor.id), method: :delete, data: {confirm: "Are you sure you want to delete this vendor?"}, class:"glyphicon glyphicon-trash", alt: "Delete Vendor" %>
    </li>

    <li>
      <%= link_to "", edit_vendor_path(vendor.id), class:"glyphicon glyphicon-pencil", alt: "Edit Vendor" %>
    </li>

  </ul>


<% end %>

</ul>

<p>
  <%# could make a "secret formula" to encode the market_id when it is passed since it will show up in the URL exposed to the user :( not sure.  See comment on _vendorform.html.erb for why we pass the market_id into the params hash %>
  <%= link_to "ADD NEW VENDOR", new_vendor_path(market_id: @market.id) %>
</p>
