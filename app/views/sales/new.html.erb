<h1>Add a new sale!</h1>

<%= form_for [@vendor, @sale] do |f| %>
  <p><%= f.label :amount %>
  <%= f.text_field :amount %></p>

  <p><%= f.label :product_id %>
  <%= f.collection_select :product_id, @vendor.products, :id, :name%></p>

  <%= f.submit %>

<% end %>
</section>
