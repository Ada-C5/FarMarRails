Please enter the dollar amount for the sale of <%=  @product.product_name %>. Enter as dollars and cents, e.g. 50.00.

<%= form_for @sale do |f| %>
  <%= f.label :amount %>
  $<%= f.text_field :amount, :required => true %>

  <%= f.hidden_field :vendor_id, :value => @vendor.vendor_id %>
  <%= f.hidden_field :product_id, :value => @product.product_id %>

  <%= f.submit %>
<% end %>

<%= link_to "Go back to my products...", product_path(@vendor.id) %>
