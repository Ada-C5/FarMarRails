<%= form_for @vendor do |t| %>

  <%= t.label :name %>
  <%= t.text_field :name %>

  <%= t.label :employee_num %>
  <%= t.text_field :employee_num %>

  <%= t.label :market_id %>
  <%= t.text_field :market_id %>

  <%= t.submit %>
<% end %>
