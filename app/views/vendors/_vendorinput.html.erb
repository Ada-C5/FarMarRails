<%= form_for ([@market, @vendor]) do |t| %>

  <%= t.label :name %>
  <%= t.text_field :name %>

  <%= t.label :employee_num %>
  <%= t.text_field :employee_num %>

  <%= t.hidden_field :market_id %>

  <%= t.submit %>
<% end %>
